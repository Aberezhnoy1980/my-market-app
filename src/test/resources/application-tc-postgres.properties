# PostgreSQL через Testcontainers (@ServiceConnection).
# Не задаём spring.datasource.* / spring.r2dbc.* — их подставляет Spring Boot из контейнера.
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
